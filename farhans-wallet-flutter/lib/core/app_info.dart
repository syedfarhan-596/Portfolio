/// Kept in sync with pubspec.yaml's `version:` on every release so the app
/// can show what's actually installed — useful for confirming a build is the
/// one you think it is, since Android will silently keep an old APK around
/// if a signature/install issue ever stops an update from landing.
const String appVersionLabel = 'Pocket Flow v1.8.0 (10)';
